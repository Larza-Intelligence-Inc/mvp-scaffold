# DEV image: optimized for fast local iteration, not production.
# The production build (multi-stage, standalone) is a separate concern — see README.
FROM node:22-alpine
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm install --legacy-peer-deps
COPY . .
EXPOSE 3001
CMD ["npm", "run", "dev"]
